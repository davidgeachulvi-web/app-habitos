# Awake — Paso 6: Depuración (backlog de errores)

**Versión:** 1.0 · septiembre 2026
**Estado:** backlog compilado y priorizado a partir de `docs/ROADMAP.md`, `docs/app-habitos.md`, la auditoría de arquitectura (`docs/04-arquitectura.md`) y la auditoría de módulos (`docs/05-modulos.md`). **Nada se ha ejecutado aún**: este documento es el acuerdo de trabajo; cada fix se hace con su verificación.

**Severidades:** 🔴 bloquea (datos/seguridad/producción) · 🟡 normal (UX, robustez) · 🟢 pulido (deuda y rendimiento).

---

## 1. Backlog priorizado

**Estado actual (Paso 6, sept 2026):** B-03 ✅ (deploy del árbol estructurado — producción sirve SW **v151** con layout por dominios), B-04 ✅ (30/30 tests), B-05 ✅ (feedback), B-06 ✅ (`docs/rls.md`), B-07 ✅ (contraste + táctiles), B-08 ✅ (reduced-motion), B-09 ✅ (aria-labels), B-18 ✅ (fuente de verdad promovida), B-19 ✅ (`.env.local` eliminado — verificado sin uso); B-20 refutado.
**Diagnóstico de BD ejecutado (`00-diagnostico.sql`, sept 2026):** la base ya tenía **casi todo aplicado** (tablas, RLS, políticas, triggers, storage, realtime de 5 tablas). **B-02 resuelto y verificado en la BD** (device_tokens, follows, badges_unlocked ✓). **B-01 resuelto y verificado en la BD** (GRANT aplicado tras ejecutar `01-pendientes-diagnostico.sql`; verificación: 5/5 en `true`). **Hallazgos del diagnóstico resueltos:** `habits.nudge` añadida y `REALTIME messages = PUBLICADO` (mismo script). **La capa social de Supabase queda 100 % operativa.** **B-24 resuelto y desplegado** (visor 3D: 3 regresiones de la promoción corregidas + gate `check-imports` reforzado) — ver fila B-24. **Deploy ejecutado (sept 2026):** producción sirve **SW v153** con el visor 3D operativo (módulos 200, rutas viejas 404, fuente corregida); `cap:copy` sincronizó Android/iOS. **Deploy del bloque UX (sept 2026):** producción sirve **SW v154** con B-25 a B-31 (toasts con tipo, navegación ARIA, insignias de DÍA diferidas, botones 3D neutros, glow progresivo, giro del «+» sincronizado); `cap:copy` sincronizó Android e iOS (app.js/app.css idénticos a `www/` verificados por checksum). **Auditoría de UX (sept 2026):** banner de logro sin repetición y sin navegación automática (desbloqueos permanentes) + **B-25 resuelto** (toasts con tipo visual éxito/error/info) + **B-26 resuelto** (navegación por pestañas con semántica ARIA completa y teclado) + **B-27 documentado** (ficha sin × en header, riesgo bajo) + **B-28 resuelto** (insignias de DÍA con entrega diferida 24 h y banner al abrir la app) + **B-29 resuelto** (botones del visor 3D con material neutro, como el botón iOS) + **B-30 resuelto** (glow progresivo en el botón «Añadir» con color del tema) + **B-31 resuelto** (giro del «+» sincronizado con el nudge) — ver filas B-25/B-26/B-27/B-28/B-29/B-30/B-31.
**NOTA (tras la promoción):** el árbol canónico `www/` tiene layout por dominios desde sept 2026; los números de línea y rutas de este backlog anteriores a la promoción son históricos (B-05: la rama vive en `www/app.js`).

| ID | Sev | Tema | Evidencia | Causa / Acción | Verificación |
|---|---|---|---|---|---|
| B-01 | ✅ | `chat_reads` sin privilegios — **RESUELTO** | 401 `permission denied` pese a existir la tabla (API PostgREST) | **Resuelto (sept 2026):** diagnóstico mostró tabla/RLS/políticas aplicadas pero GRANT en NO; ejecutado `supabase/sql/01-pendientes-diagnostico.sql` (parte 1) en el SQL Editor | ✅ Verificado: `grant_chat_reads_anon/auth/svc = true` (5/5 en la consulta de verificación) |
| B-02 | ✅ | RLS/tablas sociales — **RESUELTO (verificado en la BD)** | `device_tokens`, `follows` con 401; `badges_unlocked` 404 | **Diagnóstico (sept 2026):** `device_tokens` (tabla + RLS + policy), `follows` (3 políticas) y `profiles.badges_unlocked` (columna jsonb) **ya aplicados en la BD** | ✅ Diagnóstico: `TABLA device_tokens = EXISTE`, `RLS device_tokens = HABILITADO`, `POLÍTICAS follows = 3`, `COL profiles.badges_unlocked = EXISTE` |
| B-03 | ✅ | Producción vs disco — **RESUELTO** | ROADMAP 0.6: disco y producción desalineados | Deploys aprobados y ejecutados (sept 2026): v149 (layout plano) y **v151 (árbol estructurado por dominios, promovido)**; artefactos de desarrollo limpiados de `www/` antes del deploy | ✅ Cumplida: producción sirve **SW v151**, módulos por dominios en 200 (`js/core`, `js/domain`, `js/social`, `js/badges`, `js/vendor`), artefactos dev en 404 |
| B-04 | ✅ | Código nuevo sin tests — **RESUELTO** | Recordatorios (1.2) fuera de la red de los 16 tests | Resuelto (Paso 6): `test/recordatorios.test.mjs` con **14 tests**; mensajes de notificación extraídos a funciones puras en habits.js (4 call sites en app.js usan los helpers) | ✅ Cumplida: `npm test` **30/30** y `check-imports` OK (sept 2026) |
| B-05 | ✅ | Comentar una publicación aún no sincronizada — **RESUELTO** | `console.warn` y retorno silencioso (app.js ~9367) | Resuelto (Paso 6): feedback claro vía `mostrarToastLujo` («Publicación aún no sincronizada. Inténtalo en unos segundos.»); el texto tecleado se conserva en el input (sin pérdida). Cola local de comentarios descartada para el MVP (complejidad de sync extra) | ✅ Verificado: toast visible; smoke y tests en verde. En disco — irá a producción en el próximo deploy |
| B-06 | ✅ | 401 guest no documentado — **RESUELTO** | Consola "sucia" esperada al arrancar anónimo | Resuelto (Paso 6): `docs/rls.md` creado — mapa RLS por tabla, 401 guest documentado como comportamiento correcto, runbook de ejecución (B-01/B-02) y verificación con/sin sesión | ✅ `docs/rls.md` creado y referenciado desde `docs/06-depuracion.md` y `docs/ROADMAP.md` (5.5) |
| B-39 | ✅ | Motor de sonidos de interfaz con 4 efectos reales — **RESUELTO (implementación)** | Mejora de producto (sept 2026): sustituir los tonos sintetizados por WebAudio (B-29 previo) por efectos de sonido reales en ficheros; el usuario adjuntó 4 mp3 e indicó el punto exacto de cada uno | **Resuelto (sept 2026):** nuevo `www/js/core/sonidos.js` — motor por manifiesto `id → ruta` (respeta Ajustes → Sonido vía `awake_feedback_prefs`; no-op silencioso si el id no tiene ruta) con `reproducirSonido()` global, cache de `Audio` y tolerancia a fallos; ficheros en `www/snd/` (`sello.mp3`, `click.mp3`, `background.mp3`, `logro.mp3`) y precache en el SW (shell + regex `snd/*.mp3` para offline). **Anclajes:** `logro` → `mostrarBannerInsignia` (app.js, desbloqueo de insignia) y `mostrarHojaDiaSellado` (habits.js, día sellado con éxito); `background` → clic sobre el fondo (a la vez que la animación de estrellas, respetando `prefers-reduced-motion`, id renombrado desde `whisper`); `toque_ui` → `feedbackToqueUi` (clic en cualquier elemento, con throttling 90 ms anti doble-disparo); `sello` → `celebrarSello` (sellar una tarea); `ajustes_activar` reutiliza `click.mp3` (confirmación al activar el sonido). Los 4 sonidos de grabación de voz del chat (`chat_rec_*`) quedan sin fichero (silenciosos, pendientes) | ✅ Verificado: 4 mp3 servidos (HTTP 200, tamaños correctos), manifiesto resuelto (4 ficheros + ajustes), los 5 anclajes disparan el fichero correcto sin errores, respeto del interruptor Sonido (0 plays desactivado / 1 activado), consola limpia. Gates: 30/30 tests, sintaxis OK, check-imports OK; `cap:copy` sincronizado (Android/iOS). Sin deploy en este turno — en disco listo para el próximo (SW v163 + mp3) |
| B-40 | ✅ | Sonidos dobles: click genérico se solapaba con sello y background — **RESUELTO** | Reportado en preview (sept 2026): al sellar una tarea sonaban a la vez sello.mp3 y click.mp3; al pulsar el fondo sonaban a la vez background.mp3 y click.mp3 — cada gesto debe producir UN solo sonido | **Causa raíz (2 vías):** (1) al sellar, el `.task-checkbox` es objetivo UI → el `pointerup` global disparaba `feedbackToqueUi` (click genérico) y después su `onclick` → `celebrarSello` (sello); (2) el `<body>` lleva `onclick` propio, así que casi cualquier target (incluido el fondo) matcheaba `[onclick]` en `esObjetivoToqueUi` → click genérico + sonido de fondo a la vez | **Resuelto (sept 2026):** (1) click genérico **diferido 70 ms y cancelable** — `feedbackToqueUi` agenda `toque_ui` con `setTimeout` y `marcarSonidoEspecifico()` (llamado por sello/background/logro) lo cancela + guarda de 140 ms: si suena un específico, el click pendiente nunca llega a sonar; (2) excepción de checkbox pendiente de hábito (`toqueDisparaSello`): si el toque va a sellar, ni pointerup ni click emiten el genérico; (3) fondo evaluado PRIMERO en el click (antes que el genérico) y excluido del pointerup (`esElementoFondo` extraído de `clickEsSobreFondo`) | ✅ Verificado en vivo (preview, Audio real instrumentado): botón Ajustes → solo click.mp3 ✓; punto de fondo clasificado → solo background.mp3 ✓; desmarcar sello → solo click.mp3 ✓; sellar de nuevo → solo sello.mp3 ✓; `feedbackToqueUi`+`celebrarSello` consecutivos → solo sello.mp3 ✓ (el sello cancela el click pendiente). Gates: 30/30 tests, sintaxis OK, check-imports OK. En disco — irá a producción en el próximo deploy |
| B-41 | ✅ | Sonidos dobles en sellados async + intensidad de sonido 0-100 % + insignia «Primer día» con regla 24 h — **RESUELTO (implementación)** | Mejoras de producto (sept 2026): (1) aunque B-40 cubrió el sello por checkbox directo, los sellados con pasos async (modal «Tarea fuera del día», foto/nota con compresión + subida, 24/7) podían completar `celebrarSello` DESPUÉS de la ventana de 70 ms del click diferido → click.mp3 y sello.mp3 sonando a la vez; (2) el menú Ajustes solo permitía sonido on/off y se pide intensidad 0-100 % (0 = apagado como hoy, 100 % = encendido como hoy); (3) confirmar que la insignia «Primer día» (dia-1) nunca se entregue el mismo día sellado (B-28) | **Resuelto (sept 2026):** (1) la cancelación del click genérico se adelanta al ARRANQUE del sellado: `marcarSonidoEspecifico()` al inicio de `insertarRegistroMarca` cuando `celebrar` es true, al inicio del sello nuevo en `guardarCompletado` (antes de la compresión de imagen) y en `procesarRegistro` — el click pendiente se cancela aunque el sello tarde >70 ms (red/foto); (2) **intensidad de sonido 0-100 %**: el switch de Sonido se sustituye por un slider (`range-sonido` con relleno temático vía `--accent-hue` y % en vivo) que guarda `volumen` en `awake_feedback_prefs` (0 → `sonido:false` = antiguo off; 1-100 → `sonido:true` con ganancia proporcional `audio.volume = nivel/100` en `sonidos.js`, migración automática de prefs antiguas `sonido:false` → `volumen:0`); muestra de click al soltar el slider; (3) regla 24 h verificada: `metricasInsignias` excluye HOY (`if (i > 0) diasSellados++`) — sellar hoy no incrementa `dias` ni desbloquea dia-1 hasta el día siguiente al abrir la app | ✅ Verificado en vivo (preview): sellar E2E con checkbox real → **solo sello.mp3 (1 play)** ✓; fondo clasificado → solo background.mp3 (1) ✓; botón hamburguesa → solo click.mp3 (1) ✓; simulación async (click pendiente + sellado >70 ms) → 0 clicks, 1 sello ✓; slider 30 % → etiqueta «30%» + `audio.volume=0.3` + pref guardado; 0 % → `sonido:false`, sin reproducción; `metricasInsignias` con hoy sellado → `dias:0` (dia-1 no desbloqueable hoy) ✓. Gates: 30/30 tests (fix de id huérfano `switch-sonido` eliminado), sintaxis OK, check-imports OK; `cap:copy` sincronizado. En disco — irá a producción en el próximo deploy |
| B-42 | ✅ | Sonido de eliminación (delete.mp3) al borrar de verdad — **RESUELTO (implementación)** | Mejora de producto (sept 2026): se adjuntó `delete.mp3`; debe sonar cuando algo se elimina de verdad (tras confirmar el aviso), nunca al abrir la opción ni antes; el elemento solo desaparece cuando se confirma. Aplica a todos los borrados | **Resuelto (sept 2026):** `delete.mp3` copiado a `www/snd/` y registrado en el manifiesto de `sonidos.js` como id `eliminar`; helper global `reproducirSonidoEliminar()` en app.js (marca sonido específico —cancela el click genérico pendiente— y reproduce `eliminar`). **6 anclajes en el punto exacto del borrado** (todos ejecutan tras la confirmación del usuario): hábito (`eliminarHabitoReal`, habits.js), deseo (`eliminarDeseo` vía `confirmModalAction`, habits.js), elemento del catálogo personal (`eliminarDelCatalogoPersonal`, habits.js), registro del historial (`ejecutarEliminarRegistroHistorial`, app.js), comentario (`ejecutarEliminarComentario`, app.js) y foto de perfil (`eliminarFotoPerfil`, app.js). SW precache ampliado con `./snd/delete.mp3` (además de la regex `snd/*.mp3`) para que suene sin conexión | ✅ Verificado E2E en la preview con Audio real instrumentado: crear un deseo → ✕ → el aviso «Eliminar deseo» aparece y el deseo SIGUE visible (sin sonido delete, sin borrado prematuro) → confirmar SÍ → el deseo desaparece, el modal se cierra y suena **exactamente 1 `delete.mp3`** (sin click.mp3 en ese gesto: 5 clicks de línea base previa, +1 delete, 0 extra). Gates: 30/30 tests, sintaxis OK (app.js, habits.js, sonidos.js, sw.js), check-imports OK; `cap:copy` sincronizado (Android/iOS). En disco — irá a producción en el próximo deploy (SW v165) |
| B-43 | ✅ | Nuevo icono de la app (planeta + anillos + A) en web, Android e iOS — **RESUELTO (implementación)** | Mejora de producto (sept 2026): sustituir el icono actual por el nuevo logo elaborado (fuente `1788346804096.jpg`, 1024×1024) — planeta con anillos orbitales metálicos y «A» central sobre fondo negro; debe quedar en la PWA, en el launcher de Android y en el AppIcon de iOS | **Resuelto (sept 2026):** generación con `sharp` (lanczos3, PNG) a partir del jpg fuente; el contenido del logo ocupa ~68 % × 50 % centrado, por lo que entra en la zona segura de los iconos maskable y del launcher redondo sin recortes. **Ficheros reemplazados (mismos nombres, cero cambios de código):** web → `www/icons/icon-192.png` (192), `icon-512.png` (512), `icon-512-maskable.png` (512, full-bleed con margen seguro) y `apple-touch-icon.png` (180); iOS → `Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png` (1024, opaco sin transparencia como exige Xcode); Android → `mipmap-{mdpi,hdpi,xhdpi,xxhdpi,xxxhdpi}/ic_launcher.png` (48-192, cuadrado opaco) e `ic_launcher_round.png` (mismos tamaños, versión en disco circular con esquinas transparentes vía máscara SVG `dest-in`, replicando el formato previo del round icon). El SW no intercepta `icons/*` (solo recursos shell), así que los clientes reciben el icono nuevo sin caché vieja; no se tocó el manifest ni index.html (mismos nombres de archivo). Backup del icono anterior en `backup-icono-anterior/` (15 ficheros) | ✅ Verificado: 15/15 ficheros generados con dimensiones correctas; alfa comprobada (cuadrado opaco, round con esquinas α=0 y centro α=255, iOS opaco); inspección visual del `icon-512.png` servido por el dev server — el logo del planeta con anillos y la «A» se ve nítido sobre negro; `cap:copy` sincronizado (copias nativas de `www/icons` actualizadas en Android e iOS). En disco — el icono web se publicará con el próximo deploy; los iconos nativos requieren rebuild en Android Studio / Xcode |
| B-44 | ✅ | Ajuste de márgenes del icono (B-43): más marco en web/iOS, menos en Android — **RESUELTO (implementación)** | Feedback del usuario (sept 2026) tras B-43: al crear el acceso directo web/PWA el dibujo rozaba el marco (margen casi nulo); en el icono de Android bajado había demasiado margen negro (el logo se veía pequeño) | **Resuelto (sept 2026):** regeneración con `sharp` incorporando factor de escala del logo respecto al lienzo. **Web + iOS (s=0.85):** logo reducido — arte ~58 % × 42.5 %, márgenes reales ~21 % laterales / ~29 % verticales → al crear el acceso directo (o al escalar Chrome la zona segura del maskable) el dibujo queda claramente enmarcado. **Android (s=1.30 launcher cuadrado, s=1.15 round):** logo agrandado por zoom central (crop del cuadrado 130 % sobre el centro) — arte ~88 % × 65 %, márgenes ~5.8 % / ~17.5 % (y disco para `ic_launcher_round` que mantiene el dibujo dentro del círculo). Same 15 ficheros, mismos nombres | ✅ Verificado: márgenes medidos por bbox en cada tipo; inspección visual del `icon-512.png` servido — logo enmarcado con margen limpio; APK reconstruido (`assembleDebug` EXIT 0) y verificado por md5 que contiene el icono ajustado; deploy web **SW v166** (icono 200, 131 KB) con `cap:copy` sincronizado. En disco y producción |
| B-45 | ✅ | Splash screen nítido con el nuevo logo (en grande borroso y recortado) — **RESUELTO (implementación)** | Feedback del usuario (sept 2026): al pulsar la app en el móvil, el splash de arranque mostraba el icono EN GRANDE, borroso y recortado | **Causa raíz:** el splash nunca se actualizó al cambiar el icono. Android: el fondo de arranque usaba `@drawable/splash` con el PNG por defecto en 480×320 (¡horizontal!) → estirado a pantalla vertical y reescalado ×3 → borroso y deformado; además `windowSplashScreenAnimatedIcon` apuntaba a `@mipmap/ic_launcher` (cuadrado full-bleed) → Android 12 lo recorta al círculo → recortado. iOS: `splash-2732x2732.png` con el icono viejo llenando el cuadrado → `scaleAspectFill` mostraba un zoom recortado del arte | **Resuelto (sept 2026):** (1) `@drawable/splash` regenerado por densidad en **alta res y orientación correcta** (port 360×640→1440×2560, land girado, default 720×1280 vertical) — fondo negro + logo del planeta centrado al ~30 % del ancho (42 % del lado corto) → nítido y sin distorsión; (2) nuevo `@drawable/splash_icon` (512, disco negro con esquinas transparentes y logo grande) y `styles.xml`: `windowSplashScreenAnimatedIcon` → `@drawable/splash_icon` → en Android 12+ el círculo del sistema muestra el logo completo, sin recorte; (3) iOS: `Splash.imageset` (3 archivos 2732×2732) con fondo negro + logo al 36 % centrado (~21 % del ancho de pantalla). Backup de los splash originales en `backup-icono-anterior/android-splash-original` y `ios-splash-original` | ✅ Verificado: 15 splash generados; `assembleDebug` EXIT 0 y el APK contiene los 11 splash por densidad (24-271 KB, antes 4-17 KB) + `splash_icon` + `compat_splash_screen.xml`; referencia `@drawable/splash_icon` compilada sin errores. iOS pendiente de build en Xcode (assets en disco). En disco — Android listo para instalar |
| B-46 | ✅ | Botón «mostrar/no mostrar en perfil» sin cambio de color en el visor de insignias desde el perfil — **RESUELTO** | Reportado en preview (sept 2026): desde PERFIL → Insignias → pulsar una insignia visible en el perfil → el botón inferior de mostrar/ocultar no cambia de color al pulsarlo (no interactúa); en COLECCIÓN sí funciona | **Causa raíz:** en `htmlAccionesModalInsignia` (app.js) la rama del botón usaba `if (modo === 'showcase' || enShow)` para la variante verde «No mostrar en perfil» y `if (modo === 'coleccion' && !enShow)` para la roja «Mostrar en perfil». Al abrir el visor desde el perfil (`badgeVistaActiva = 'showcase'`), la primera condición se cumplía SIEMPRE (por el `modo === 'showcase'`), así que al alternar la vitrina el re-render volvía a pintar la misma variante verde → sin cambio visual | **Resuelto (sept 2026):** el botón ahora se decide SOLO por el estado real `estaEnShowcase(item.id)` (verde si está en perfil, roja si no), independientemente del modo desde el que se abra el visor (colección o perfil). Al pulsar, `alternarShowcaseInsignia3D` re-renderiza la acción y el botón refleja el nuevo estado (verde ↔ rojo), igual que en colección | ✅ Verificado E2E en la preview (visor abierto desde PERFIL → Insignias con `sello-1` realmente desbloqueado): antes → `is-showing` «No mostrar en perfil» (verde rgb(139,224,176)); tras pulsar → `is-hidden` «Mostrar en perfil» (**rojo rgb(240,161,161)**), `enShow` flipped a false; y el estado restaurado. Gates: 30/30 tests, sintaxis OK, check-imports OK; `cap:copy` sincronizado (Android/iOS). En disco — irá a producción en el próximo deploy (SW v167) |
| B-47 | ✅ | Splash Android: icono cortado a los lados tras B-45 — **RESUELTO (implementación)** | Feedback del usuario (sept 2026) tras B-45: el icono del splash ya se ve en mejor resolución, pero el planeta y sus anillos siguen apareciendo CORTADOS por ambos costados | **Causa raíz:** el `@drawable/splash_icon` (icono del splash de Android 12+) contenía el planeta al **76 % × 56 %** del lienzo; dentro de la máscara circular del sistema (y con el zoom extra que aplican algunos launchers/OEM) los anillos que sobresalen en horizontal quedan fuera del círculo visible → recorte por los dos lados | **Resuelto (sept 2026):** `splash_icon` regenerado con el arte a 400 px sobre el lienzo de 512 → planeta al **54 % × 40 %** (disco negro y esquinas transparentes intactos); ahora el logo cabe completo en la máscara circular aunque el launcher haga zoom (hasta ~1.35× el planeta ocupa ~73 % del círculo, sin cortes). Los splash legacy (`@drawable/splash`, logo al 30 %) e iOS (21 %) ya eran seguros | ✅ Verificado: bbox medido (76 % → 54 % ancho); `assembleDebug` EXIT 0 y el APK contiene el `splash_icon` nuevo (md5 idéntico, 120 KB). Android listo para instalar; sin cambios en web/iOS |
| B-48 | ✅ | Fotos en sellos sin iniciar sesión (invitado) — **RESUELTO (implementación)** | Petición de producto (sept 2026): al sellar un hábito y adjuntar una foto, un usuario NO registrado no debe ver el aviso «Inicia sesión para guardar la foto»; la foto debe quedar en el REGISTRO/HISTORIAL (imagen + nota + fecha + título de la tarea), sin publicarse ni llevar likes/comentarios | **Resuelto (sept 2026):** (1) `subirImagenOAvisar` (app.js): si no hay `currentUser` devuelve la foto como data URL local (sin subir a Supabase y sin toast); (2) `procesarRegistro` y `aplicarUpdate` (habits.js): solo se descartan los data URLs si hay cuenta (`&& currentUser`) — los registros de invitado conservan su imagen; (3) `serializarHistorial`: conserva las data URLs de los registros locales (sin user_id) al persistir → la foto sobrevive a recargas; (4) el feed vertical del detalle marca `.ig-post-local` cuando `pub.user_id` es null → CSS oculta la fila de likes, la zona de comentarios y el input (sin corazones ni comentarios, tampoco doble-toque de like); (5) la galería «publicaciones» del perfil propio excluye los registros locales (`esSelloVisibleEnRed(ej) && !!ej.user_id`) → no aparecen como publicación. Queda intacta: imagen + nota + fecha + título en el Historial | ✅ Verificado E2E en la preview (flujo REAL de invitado): sellar por checkbox → aparece el botón de cámara → adjuntar foto + «Mi nota E2E» → Guardar → log con `image_url` **data URL (9.567 chars)**, `user_id: null`, **sin toast «Inicia sesión»**; Historial muestra la miniatura con la imagen; el detalle abre con clase `ig-post-local` y acciones/comentarios/input en `display:none`; título + nota visibles; perfil → publicaciones «Galería vacía» (0 publicaciones); foto persistida en `monolith_app_state:guest`. Gates: 30/30 tests, sintaxis OK, check-imports OK; `cap:copy` sincronizado (Android/iOS). En disco — irá a producción en el próximo deploy (SW v167) |
| B-49 | ✅ | Iconos SVG en los títulos de sección (SELLO/DÍA/RACHA/MEMORIA) y ojo verde en insignias mostradas en el perfil (colección) — **RESUELTO** | Petición de producto (sept 2026): en el apartado COLECCIÓN, los títulos de cada sección de insignias (sello, día, racha, memoria) deben incluir su icono SVG para identificarse más rápido; y junto al título de cada insignia mostrada en el perfil debe aparecer también un icono SVG indicador | **Resuelto (sept 2026):** en `pintarGridInsignias` (app.js) (1) cada bloque de sección añade `.badge-theme-icon` con `svgPhosphorPorClave` teñido por el acento de la sección (`--badge-accent`): sello→`check` (✓), día→`sun-horizon` (☀ horizonte), racha→`flame`, memoria→`camera` (18 px); (2) cada insignia desbloqueada Y en la vitrina del perfil (`!ajeno && item.ok && estaEnShowcase(item.id)`) muestra `OJO_ABIERTO_SVG` en `.badge-in-profile` (verde rgb(139,224,176), 13 px) junto al título. CSS en app.css (`.badge-theme-icon`, `.badge-in-profile`) | ✅ Verificado E2E en la preview: los 4 títulos de sección muestran su icono coloreado con el acento (sello ✓ #c9b896, día ☀ #e0a46a, racha 🔥 #e07a4a, memoria 📷 #7eb0c8); con `sello-1` desbloqueado y en vitrina, «Primer sello · Encendida» muestra el ojo verde al lado del título, y las insignias no mostradas NO lo muestran. Gates: 30/30 tests, sintaxis OK, check-imports OK; `cap:copy` sincronizado (Android/iOS). En disco — irá a producción en el próximo deploy (SW v167) |
| B-50 | ✅ | Foto de invitado aparecía en SOCIAL → ACTIVIDAD — **RESUELTO** | Reportado por el usuario (sept 2026): tras B-48 (fotos sin iniciar sesión), la imagen adjuntada por un usuario NO registrado aparecía en la sección «Actividad» del menú SOCIAL; debe quedar SOLO en HISTORIAL → Registros, totalmente aislado del apartado social | **Causa raíz:** `renderizarFeedActividad` (app.js) recorría el `historialAgrupado` PROPIO y admitía cualquier log con imagen que pasara `esSelloVisibleEnRed` (que solo mira `privacidad`). Los sellos de invitado se guardaban con `privacidad: 'seguidores'` (default) → la foto del invitado (sin `user_id`) entraba al feed social. La regla correcta ya existía en `renderizarPerfilPublicacionesGrid` (`esSelloVisibleEnRed(ej) && !!ej.user_id` para el propio perfil) | **Resuelto (sept 2026):** en el bloque del historial propio de `renderizarFeedActividad` ahora se exige `!!ej.user_id`: un registro sin cuenta (invitado, `user_id: null`) jamás entra en el feed ACTIVIDAD, tenga o no foto. El bloque de `cachePerfilesSocial` (perfiles ajenos, siempre con user_id real de Supabase) no requiere cambios; `publicacionesDeLogsHabito` (ficha del hábito dentro de HISTORIAL) conserva los registros locales | ✅ Verificado E2E en la preview (invitado con sello + foto + nota, `privacidad: 'seguidores'`, `user_id: null`): feed ACTIVIDAD → **«Sin actividad» (0 publicaciones, sin la imagen)** ✓; HISTORIAL → REGISTROS → acordeón «Leer · 1 registro» con **imagen + nota + fecha** ✓. Gates: 30/30 tests, sintaxis OK, check-imports OK; `cap:copy` sincronizado (Android/iOS). En disco — irá a producción en el próximo deploy (SW v167) |

| B-51 | ✅ | Botones de empty state «AÑADE TU PRIMER HÁBITO/DESEO» en mayúsculas — **RESUELTO** | Petición de producto (sept 2026): el contenedor animado que incita a añadir el primer hábito (y el equivalente de deseos) debe escribirse TODO en mayúsculas, sin variar nada más | **Resuelto (sept 2026):** en `htmlBotonAnadir` (habits.js) los textos «Añade tu primer hábito» / «Añade tu primer deseo» pasan a mayúsculas vía literal («AÑADE TU PRIMER HÁBITO/DESEO»), manteniendo clase, animación y comportamiento intactos | ✅ Verificado por inspección del render y gates (30/30 tests, sintaxis OK, check-imports OK). En disco — irá a producción en el próximo deploy (SW v168) |
| B-52 | ✅ | Icono del menú hamburguesa → engranaje con giro al abrir — **RESUELTO** | Petición de producto (sept 2026): sustituir el icono hamburguesa (tres líneas) de la esquina superior derecha por un engranaje; debe notarse que el menú abre | **Resuelto (sept 2026):** en index.html el SVG del botón pasa a ser un engranaje (`gear`, stroke) con clase `menu-gear`; se retiraron las reglas de las líneas y se añadió animación de rotación al abrir (`.is-open .menu-gear` gira ~120°) con `prefers-reduced-motion` respetado | ✅ Verificado por inspección y gates (30/30, sintaxis, check-imports). En disco — irá a producción en el próximo deploy (SW v168) |
| B-53 | ✅ | Menú de ajustes rediseñado: índice con tarjetas + pantalla única por sección — **RESUELTO** | Petición de producto (sept 2026): el menú debe abrirse en pantalla completa y sin abrumar; dividir por apartados (Cuenta, Apariencia, Interacción, «Ritual» → «Avisos», Provisional): cada sección en un contenedor y, al entrar, solo esa sección aislada con ← Volver | **Resuelto (sept 2026):** el modal de ajustes pasa a dos niveles. Nivel 1: índice (`#settings-index.active`) con 5 tarjetas (icono SVG + título + descripción). Nivel 2: vistas `#settings-view-{cuenta,apariencia,interaccion,avisos,provisional}` (una activa a la vez), con el botón circular ← en el header (`#settings-back-btn`) y título de sección. `abrirSeccionAjustes()/volverIndiceAjustes()` + `ajustesSeccionActual` (persiste la sección al cerrar/reabrir por capas como el modal de tema). Todos los IDs de sincronización (`range-sonido`, `switch-*`, `digest-*`, `menu-login-btn`, `settings-logout-wrap`) intactos dentro de sus vistas; la sección Ritual ahora se llama «Avisos» | ✅ Verificado E2E en la preview: 5 tarjetas en el índice; entrar en una sección → única vista activa (título en header, ← visible, resto ocultas); volver → índice. **Ajuste del usuario:** al cerrar y volver a entrar en el engranaje SIEMPRE se muestra el índice (la última sección ya no se recuerda — se eliminó `ajustesSeccionActual`); y el estilo de las tarjetas pasó de acento de tema + gradiente/sombra a tonos neutros del tema por defecto (fondo plano rgba(255,255,255,0.06), iconos `--text-muted`, borde `--border-subtle`, sin acento ni sombra). Icono de la tarjeta «Apariencia»: el sparkle de Phosphor proporcionado por el usuario (path `M232,32…`, viewBox 256) renderizado con `fill=currentColor` para conservar el tono neutro. **Ajuste del tema:** el borde de las tarjetas del índice y de los chips pasa de `--border-subtle` (nunca teñido) a `--border-metal` (variable que `aplicarTemaGlobalHabitos` re-tiñe con el hue elegido en Apariencia) → en reposo, sin hover (app móvil), los contenedores adoptan el color del tema como el resto de la app. Gates: 30/30 tests, sintaxis OK (app.js/habits.js), check-imports OK; `cap:copy` sincronizado. En disco — irá a producción en el próximo deploy (SW v168) |
| B-54 | ✅ | Selector de imagen de sellos/deseos intuitivo («Adjuntar imagen a la tarea») — **RESUELTO** | Petición de producto (sept 2026): el input `<input type=file>` nativo («Seleccionar archivo · Ningún archivo seleccionado») no es intuitivo; debe quedar claro que ahí se adjunta una imagen a la tarea | **Resuelto (sept 2026):** ambos inputs (`#completion-image` en sellos y `#wish-completion-image` en deseos) quedan envueltos en `.attach-image-label`: icono de cámara en círculo con color del tema, título «Adjuntar imagen a la tarea» / «Adjuntar imagen al deseo» y pista «Toca para elegir o tomar una foto»; el input se mantiene funcional oculto (clip, no display:none) para que el label abra el selector y el JS existente (`files[0]`, resets) siga intacto. Al elegir archivo, el título muestra el nombre y la zona pasa a `.has-file` (borde sólido). Accesible (tabindex + Enter/Space). Los 3 puntos donde el JS resetea el input re-sincronizan la etiqueta | ✅ Verificado en la preview: ambas zonas presentes con título/pista correctos, input oculto funcional. Gates: 30/30 tests, sintaxis OK (app.js/habits.js), check-imports OK. En disco — irá a producción en el próximo deploy (SW v168) |
| B-55 | ✅ | Insignia de «memoria» (y resto): estado `ok` incoherente entre colección y vitrina en invitado — **RESUELTO** | Reportado durante la investigación (sept 2026): con fotos de invitado (B-48) el desbloqueo guardado no coincidía con lo que mostraba la colección —el `ok` se decidía por la MÉTRICA en vivo en `sincronizarInsigniasConMetricas`, no por el desbloqueo guardado—, descuadrando banner, vitrina, colección y visor 3D | **Resuelto (sept 2026):** en `sincronizarInsigniasConMetricas` (app.js) el `ok` de cada item ahora se decide por `!!saved[def.id]` (desbloqueo GUARDADO) en lugar de `prog.ok` (métrica en vivo); el desbloqueo sigue ocurriendo por métrica (`if (prog.ok && !ya)`) una sola vez y de ahí en adelante es permanente. La vitrina/perfil ya usaban la fuente guardada (`itemsInsigniasDesdeMapa`) → ahora colección y perfil siempre coinciden | ✅ Verificado E2E en la preview: con `sello-1` guardado SIN métrica (sellos=0), el catálogo lo muestra `ok:true`; antes habría mostrado `ok:false`. Gates: 30/30 tests, sintaxis OK, check-imports OK. En disco — irá a producción en el próximo deploy (SW v168) |
| B-56 | ✅ | Revocar insignias al ELIMINAR de verdad la tarea causante (conservar al deshacer sello) — **RESUELTO** | Indicación de producto (sept 2026): al eliminar la tarea/registro que causó un desbloqueo, la insignia debe desaparecer (y salir de la vitrina del perfil); deshacer un sello la mantiene (decisión B-41/B-46) | **Resuelto (sept 2026):** nueva `revocarInsigniasSinMetrica(opts)` en app.js: recalcula `metricasInsignias()` y retira todo desbloqueo guardado cuyo umbral ya no se cumple (también de `_showcase`), guarda y persiste en nube — sin banner. Enganchada en los BORRADOS REALES: `eliminarHabitoReal` y `eliminarDeseo` (habits.js, con `renderizarInsignias()` para refrescar) y `ejecutarEliminarRegistroHistorial` (app.js). Deshacer sello/omisión/recaída (`borrarLogDelDia`) NO pasa por aquí → el desbloqueo se mantiene | ✅ Verificado E2E en la preview: con `sello-1` y `memoria-1` guardados sin métrica → `revocarInsigniasSinMetrica()` devuelve `['sello-1','memoria-1']`, desaparecen del mapa guardado y del catálogo (`ok:false`). Gates: 30/30 tests, sintaxis OK (app.js/habits.js), check-imports OK; `cap:copy` sincronizado. En disco — irá a producción en el próximo deploy (SW v168) |
| B-57 | ✅ | Auditoría de coordinación hábitos ↔ registros ↔ social ↔ insignias: revocar insignias al deshacer un sello — **RESUELTO** | Revisión profunda de producto (sept 2026): comprobar que toda la lógica (crear/sellar/desellar/eliminar hábitos y deseos) está coordinada con el registro, el feed social de publicaciones y las insignias; «si completamos un hábito y nos recompensan con una insignia, si deshacemos el hábito la insignia también debe revertirse» — en todo momento todo coordinado y actualizado | **Hallazgos de la auditoría:** 1) `revocarInsigniasSinMetrica` (B-56) solo estaba en los BORRADOS reales; al DESHACER un sello (checkbox, 24/7, recaída, undo) la insignia se mantenía encendida aunque la métrica ya no se cumpliera — justo lo que el usuario pidió revertir (supersede la decisión B-41/B-46 de «desbloqueo permanente»). 2) El feed ACTIVIDAD no se refrescaba al volver a la pestaña SOCIAL si el grid ya tenía contenido → podía quedar una publicación de un sello ya deshecho/eliminado. 3) `guardarCompletacionDeseo` llamaba `revocarInsigniasSinMetrica()` (mal ubicado: completar un deseo no revoca) y `renderizarInsignias()` dos veces seguidas (duplicado). 4) `desmarcarDeseo` y `eliminarDeseo` no revocaban (los logs de deseo cuentan en métricas `sellos`/`fotos`). 5) Al revocar, no se limpiaba el banner/toast pendiente de esa insignia | **Resuelto (sept 2026):** 1) `revocarInsigniasSinMetrica()` ahora se ejecuta tras DESELLAR (ruta normal, 24/7 y deshacer recaída en `ejecutarLogicaCheckboxHabito`) y en `deshacerUltimoSellado` (app.js) → al quitar el sello, las insignias cuyo umbral ya no se cumple se revierten en silencio (sin banner). 2) `cambiarTab(2)` limpia `#social-activity-grid` si ACTIVIDAD está activa → el feed se re-renderiza fresco al volver a Social (no quedan publicaciones obsoletas). 3) Limpieza en deseos: se retira el revoke mal ubicado y el doble render de `guardarCompletacionDeseo`; se añade revoke a `desmarcarDeseo` (quitar cumplido) y `eliminarDeseo`. 4) `revocarInsigniasSinMetrica` limpia `badgeToastPendiente` y `insigniasPendientesSello` cuando revoca (el aviso de una insignia revocada no debe llegar). Nota: la ventana «Deshacer» se cierra al sellar (decisión previa B-46), así que `deshacerUltimoSellado` es vía de respaldo; el camino real de desellado es el checkbox y queda cubierto | ✅ Verificado E2E en la preview (flujo real): sellar → log creado + `sello-1` desbloqueado + banner «Primer sello · Hierro» ✓; desellar (checkbox + confirmar) → log eliminado, `sello-1` REVOCADO (fuera del mapa guardado y `ok:false`), SIN banner ✓; re-sellar → `sello-1` vuelve a desbloquearse con banner ✓. Banner pendiente limpiado al revocar ✓. Feed ACTIVIDAD refrescado al volver a Social (tarjeta fantasma eliminada) ✓. Gates: 30/30 tests, sintaxis OK (app.js/habits.js), check-imports OK (12 módulos); `cap:copy` sincronizado (Android/iOS). En disco — irá a producción en el próximo deploy (SW v168) |
| B-58 | ✅ | Sonido de eliminar al quitar el sello de una tarea — **RESUELTO** | Indicación de producto (sept 2026): al QUITAR el sello de una tarea debe sonar el mismo efecto de eliminación (delete.mp3) que al borrar algo — siempre en el momento en que el sello se elimina de verdad (tras confirmar), nunca al pulsar el checkbox | **Resuelto (sept 2026):** `reproducirSonidoEliminar()` se invoca dentro de `desellarSellosHabitoEnFecha` (habits.js), justo después de confirmar que hay sellos que quitar (`if (!logs.length) return 0`) y antes del borrado. Como TODOS los desellados pasan por esa función (checkbox normal, sello 24/7, deshacer recaída/borrarLogDelDia), el sonido suena en todos los caminos al momento real de la eliminación. `reproducirSonidoEliminar` ya cancela el click genérico pendiente (`marcarSonidoEspecifico`) → único sonido del gesto, sin dobles | ✅ Verificado E2E en la preview con stub de Audio (flujo real: tarjeta sellada → clic checkbox → confirmar «Quitar el sello»): se registró **una sola reproducción de `snd/delete.mp3`** (sin click.mp3 duplicado) y el sello quedó eliminado del historial. Gates: 30/30 tests, sintaxis OK, check-imports OK (12 módulos); `cap:copy` sincronizado (Android/iOS). En disco — irá a producción en el próximo deploy (SW v168) |
| B-59 | ✅ | Aviso de confirmación al quitar el sello también en hábitos «Todo el día / 24/7» — **RESUELTO** | Reportado por el usuario (sept 2026): al pulsar el sello ya aplicado a una tarea debe salir SIEMPRE el aviso «¿seguro que quieres quitar el sello?». El aviso existía y sigue existiendo en tareas normales (Mañana/Tarde/Noche) y en abstinencias (modal «Hoy recaí»), pero en hábitos rituales de tipo «Todo el día / 24/7» el desellado era INSTANTÁNEO, sin confirmación | **Resuelto (sept 2026):** en `ejecutarLogicaCheckboxHabito` (habits.js), la rama `momento === '24/7'` con sello puesto ahora abre el mismo modal de confirmación que el desellado normal (`confirm-modal` con `copia.desellarTitulo`/`copia.desellarTexto`, «Quitar el sello» / «Se elimina este registro del día, incluida cualquier foto.») y solo elimina el sello al confirmar con «Sí». El resto de la cadena (racha, nube, renders, revocación B-57, sonido de eliminar B-58) se conserva dentro de `confirmModalAction` | ✅ Verificado E2E en la preview (hábito ritual 24/7 sellado → pulsar el ✓): el aviso «Quitar el sello» SALE (antes se desellaba al instante), el sello NO se elimina hasta confirmar, y con «Sí» desaparece del historial y el checkbox vuelve a vacío. Smoke de regresión: la ruta normal (MAÑANA) sigue mostrando el modal. Gates: 30/30 tests, sintaxis OK (app.js/habits.js), check-imports OK (12 módulos); `cap:copy` sincronizado (Android/iOS). En disco — irá a producción en el próximo deploy (SW v168) |
| B-60 | ✅ | Mensajes de confirmación específicos y claros al quitar sellos (tarea/hábito, actividad, abstinencia, deseo) — **RESUELTO** | Petición de producto (sept 2026): perfeccionar los avisos de confirmación para que indiquen el TIPO de elemento cuyo sello se va a quitar («un sello de una tarea (hábitos), de un deseo…») y aporten la información clara del caso | **Resuelto (sept 2026):** nueva función central `textoConfirmacionQuitarSello(opts)` en habits.js que genera título y texto según el tipo y los datos reales: **tarea/hábito** → «Vas a quitar el sello de la tarea «Leer» · Mañana. Se eliminará el registro de ese día (incluida su foto o nota).» (momento Mañana/Tarde/Noche; «Todo el día» en 24/7); **actividad puntual** → «de la actividad «X»…»; **abstinencia** → «Vas a marcar una recaída en «X». Se quitará el sello del día (si estaba sellada). La racha se romperá.»; **deseo** → «Quitar el sello del deseo: Vas a quitar el sello del deseo «X». Volverá a quedar pendiente y su registro saldrá del historial.» El sufijo «(incluida su foto o nota)» solo aparece cuando el registro realmente tiene adjuntos (se inspecciona el log/deseo). Además, DESMARCAR un deseo cumplido ya no es instantáneo: ahora también abre el aviso de confirmación (nueva `solicitarDesmarcarDeseo`) | ✅ Verificado E2E en la preview: textos correctos para los 5 casos (tarea normal, 24/7, actividad, abstinencia sellada/sin sello, deseo con/sin adjuntos); tarea con foto muestra «(incluida su foto o nota)»; deseo cumplido → aviso «Quitar el sello del deseo» y al confirmar vuelve a pendiente. Gates: 30/30 tests, sintaxis OK (app.js/habits.js), check-imports OK (12 módulos); `cap:copy` sincronizado (Android/iOS). En disco — irá a producción en el próximo deploy (SW v168) |
| B-07 | ✅ | Contraste y táctiles — **RESUELTO** | `--text-dim` #6b7280 daba ≈ 3.7:1 sobre #121824 (fallaba); `badge-nav-btn` 42 px | Resuelto (Paso 6): `--text-dim` → **#98a1b3** (≈ 6.8:1); `badge-nav-btn` → **44 px**; `min-height: 44px` en `.day-btn`, `.filter-pill` y `.settings-row`. No hay tema claro (fondos oscuros únicos) | ✅ Verificado: `npm test` 30/30; contraste y táctiles dentro de WCAG |
| B-08 | ✅ | Movimiento reducido — **RESUELTO** | Modales y visor con animaciones de entrada (`badge-modal-in`, `badge-coin-leave/enter`, `toast-in`) | Resuelto (Paso 6): **premisa corregida** — la moneda no tiene rotación automática (solo drag del usuario). Neutralizadas bajo `prefers-reduced-motion`: animaciones del visor, entrada del modal de insignias y del toast | ✅ Verificado: con la preferencia activa no hay transiciones de entrada bruscas |
| B-22 | ✅ | `REALTIME messages = NO` — **RESUELTO** | El cliente se suscribe a `postgres_changes` en `messages` (`js/social/chat.js` 1385–1389) pero la tabla no estaba en `supabase_realtime` | Ejecutado `01-pendientes-diagnostico.sql` (parte 3) | ✅ Verificado: `realtime_messages = true` |
| B-23 | ✅ | `COL habits.nudge = FALTA` — **RESUELTO** | Columna del ritual (Fase 1.1) ausente; el código aún no la usa (solo `glyph` está vivo) | Ejecutado `01-pendientes-diagnostico.sql` (parte 2); completitud de esquema | ✅ Verificado: `col_habits_nudge = true` |
| B-24 | ✅ | Visor 3D de insignias roto — **RESUELTO (3 causas)** | «las monedas 3D no aparecen» (reportado en preview sept 2026): `Failed to fetch dynamically imported module` + `MIME type text/html` | **Causa raíz (regresiones de la promoción B-18):** (1) `app.js` importaba la ruta vieja `./js/badge-coin-3d.js` (→ `js/badges/badge-coin-3d.js`); (2) **`three.core.min.js` ausente** — el grafo de Three r185 lo importa y nunca se copió al árbol estructurado (el dev server devolvía el fallback HTML → el grafo entero fallaba); (3) fuente `Cinzel-SemiBold.woff2` con ruta relativa rota (`../../fonts/` desde `js/badges/coin/` resolvía a `js/fonts/` — debe ser `../../../fonts/`). **Bonus:** el gate `check-imports.js` no detectaba imports minificados sin espacios (`import{...}from"..."`) ni archivos faltantes — reescrito para verificar existencia (12 módulos) | ✅ Verificado en preview (motor 3D `_ready`, canvas 352×352, moneda renderizada, fuente sin errores) y **desplegado**: producción sirve SW v153, módulos del visor en 200, rutas rotas en 404, `check-imports` OK (12 módulos), `npm test` 30/30 |
| B-25 | ✅ | Avisos sin jerarquía visual — **RESUELTO** | Auditoría de UX (sept 2026): los ~40 avisos usaban un único estilo neutro — un error grave, una validación y una confirmación se veían idénticos | **Resuelto (auditoría UX, sept 2026):** `mostrarToastLujo` acepta `opts.tipo` (`exito`/`error`/`info`) con icono SVG + color de acento + fondo propio (verde #4ade80 ✓, rojo #f87171 ⚠, gris #94a3b8 ℹ); los ~40 call sites clasificados (errores de red/validación → `error`, confirmaciones → `exito`, coach/notas → `info`); banner de insignia usa `exito` en su fallback; acceso por icono `aria-hidden` | ✅ Verificado en preview (3 tipos con su color e icono, sin tocar navegación) y `npm test` 30/30; en disco — irá a producción en el próximo deploy |
| B-26 | ✅ | Navegación por pestañas sin semántica ARIA — **RESUELTO** | Auditoría de navegación (sept 2026): el bottom nav, los sub-tabs del perfil y los modal-tabs (auth, seguidores) eran `<div onclick>` sin `role="tab"`, `aria-selected` ni `tabindex` — los lectores de pantalla y el teclado no los reconocían como pestañas (contradecía el B-09) | **Resuelto (auditoría UX, sept 2026):** `role="tab"` + `aria-selected` + roving tabindex (`0` activo / `-1` resto) en nav principal (`sincronizarARIANavPrincipal`), sub-tabs del perfil (`cambiarSubTabPerfil`), modal-tabs de auth (`cambiarAuthTab`) y seguidores (`cambiarSocialTab`); navegación por teclado añadida (flechas ←/→ y Home/End en el patrón tablist) | ✅ Verificado en preview (tab activo con `aria-selected=true` y `tabindex=0`; flecha derecha activa y enfoca la siguiente pestaña; sub-tabs del perfil sincronizan al hacer click) y `npm test` 30/30 |
| B-31 | ✅ | Giro del «+» desincronizado del nudge — **RESUELTO** | Reportado en preview (sept 2026): el giro de 90° del «+» en el botón del primer hábito/deseo quedaba «colgado» tras el nudge del contenedor — la desincronización era real en CSS: `add-first-plus` mantenía `rotate(90deg)` hasta el 100 % del ciclo mientras `add-first-nudge` reposaba en el 88 % (~0.6 s de desfase) | **Resuelto (sept 2026):** keyframes `add-first-plus` corregido — `88%, 100% → rotate(0deg)` — el «+» vuelve a 0° exactamente cuando el contenedor reposa (misma ventana 70→78→88 %). Verificado con muestreo rAF: ambos elementos inician y reposan a la vez (t=3213 ambos en movimiento; t=4019 ambos `matrix(1,0,0,1)`) | ✅ Verificado en preview: sincronización exacta contenedor/«+», sin giro colgado; 30/30 tests, sintaxis OK. En disco — irá a producción en el próximo deploy |
| B-30 | ✅ | Glow progresivo en «Añadir hábito/deseo» — **RESUELTO** | Mejora visual (sept 2026): el botón del primer hábito/deseo debe incitar de forma más llamativa con un glow que crezca durante la animación y siga el color del tema | **Resuelto (sept 2026):** keyframes `add-first-glow` (5 s, sincronizado con el nudge) — el brillo crece de 0 %→78 % (pico: alfa 0.6, 22 px + 46 px) y se apaga; color vía variable `--glow-accent` (por defecto `210, 90%, 62%`, el tema la redefine con `hsl(hue, 95%, 62%)`); eliminado el `box-shadow !important` temático que mataba la animación; respeta `prefers-reduced-motion` (heredado de `.is-first`) | ✅ Verificado en preview: animación `add-first-glow` activa, muestreo progresivo (0 %: alfa 0.16 · 70 %: 0.34 · 78 %: 0.6), color del tema aplicado (hue 271 púrpura), captura visual con glow. Gates: 30/30 tests, sintaxis OK. En disco — irá a producción en el próximo deploy |
| B-29 | ✅ | Botones del visor 3D teñidos con el tema — **RESUELTO** | Auditoría visual (sept 2026): los botones anterior/siguiente del visor de insignias 3D se pintaban con el color del tema en su relleno interior (`hsl(hue)` + gradiente + `!important`), a diferencia del `#ios-back-btn` (referencia de la adaptación iOS) que usa variables CSS neutras | **Resuelto (sept 2026):** eliminada la regla temática `.badge-nav-btn` del bloque de tema en `app.js`; el fondo del botón ahora usa `--surface-luxury-strong` (el mismo material que `#ios-back-btn`) con color de texto `--text-main` neutro — el único tono que comparte es `--border-metal`, igual que el botón iOS (consistente) | ✅ Verificado en preview con tema activo: fondo y texto neutros idénticos al botón iOS (`linear-gradient(rgba(18,24,36,0.55)...)`); visor 3D operativo; `npm test` 30/30. En disco — irá a producción en el próximo deploy |
| B-32 | ✅ | Detalles premium promovidos del prototipo — **RESUELTO (promoción)** | Prototipo visual `proto/ui.html` revisado por el responsable de producto (sept 2026): quedarse con el checkbox circular, el reflejo que barre solo en tareas pendientes y la onda circular al sellar; el resto del catálogo experimental se descartó | **Resuelto (sept 2026):** promovido a `www/app.css` — `.task-checkbox` circular (50 %, 32 px); reflejo `shine-sweep` con exclusión `:not(.is-done)` (solo pendientes) implementado con `background-position` en `::before` (sin `overflow: hidden`, para no recortar el menú contextual de la tarjeta); onda circular `seal-ring-circ` (anillo 50 % + halo con `--accent-hue`, variable inyectada en el `:root` del tema en `app.js` — fallback 195 = acento base); respeta `prefers-reduced-motion` | ✅ Verificado en la app real (`localhost:4173`): checkbox 50 %·32 px ✓; `shine-sweep` en tarjeta pendiente y NONE en `.is-done` ✓; overflow de tarjeta `visible` (menú ••• intacto) ✓; anillo `seal-ring-circ` con radio 50 % ✓; `--accent-hue` del tema real del usuario (161) aplicado ✓; gates: 30/30 tests, sintaxis OK, check-imports OK. En disco — irá a producción en el próximo deploy |
| B-33 | ✅ | Selección de día en la franja del calendario — **RESUELTO** | Reportado en preview (sept 2026): al pulsar un día de la barra horizontal (pasado/futuro) no se seleccionaba — «no se está seleccionando el día que pulso» | **Causa raíz:** el carrusel usa `setPointerCapture` en `pointerdown` y el navegador retargetea el `click` derivado al viewport capturado (verificado en vivo: el `click` aterrizaba en `calendar-strip-viewport`, nunca en la celda `.cal-day`) — el `onclick` del día no se ejecutaba ni con dedo ni con ratón | **Resuelto (sept 2026):** selección extraída a `seleccionarDiaCalendario()` (fecha guardada en `dayDiv._fecha`); el `pointerup` del carrusel resuelve el toque limpio (dx/dy ≤ 10 px) con `elementsFromPoint` y selecciona la celda; guarda `calStripDidTap` (350 ms) contra doble selección cuando el click sí llega a la celda. El swipe semanal y el botón «Volver a hoy» intactos | ✅ Verificado en preview con clic real: día 3 seleccionado ✓; día 10 de la semana siguiente ✓; swipe completo cambia de semana (31-8 → 7-9) sin seleccionar ✓; gates: 30/30 tests, sintaxis OK, check-imports OK; `cap:copy` sincronizado (Android/iOS). En disco — irá a producción en el próximo deploy |
| B-34 | ✅ | Navegación del visor según entrada (social vs colección) — **RESUELTO** | Criterio de producto (sept 2026): al acceder a las insignias desde la interfaz de social (perfil), deslizar entre insignias debe mostrar **solo las visibles en el perfil**; desde COLECCIÓN se mantiene la serie completa del tema | **Resuelto (sept 2026):** nueva `construirNavegacionVisorInsignia(item)` — si `badgeVistaActiva === 'showcase'` (acceso por perfil/vitrina), `badgeNavIds` se filtra por la vitrina del perfil (`itemsShowcaseDesdeMapa`, propio o ajeno vía `cachePerfilesSocial`; fallback con la insignia en pantalla si no estuviera en la lista); si es 'coleccion', serie completa del tema como antes. Extraído `pintarNavegacionVisor()` (contador y botones prev/next) y refrescado al alternar «Mostrar/No mostrar en perfil» sin cerrar el visor (anclado en la insignia en pantalla) | ✅ Verificado en preview con estado inyectado (4 sellos, vitrina sello-1+sello-3): colección → contador «1 / 6»; perfil → «1 / 2» (nav `[sello-1, sello-3]`), siguiente → «2 / 2» (sello-3), anterior → «1 / 2»; ocultar sello-3 con el visor abierto actualiza al instante («1 / 1», next oculto); reapertura coherente. Gates: 30/30 tests, sintaxis OK, check-imports OK; `cap:copy` sincronizado. En disco — irá a producción en el próximo deploy |
| B-35 | 🟡 | Visor 3D roto en iOS (iPhone/Safari + app nativa) — **EN CURSO (hardening aplicado, validación en dispositivo pendiente)** | Reportado (sept 2026): la moneda 3D de insignias funciona en PC/Android pero en iOS no llega a abrirse o renderizarse; sin iPhone ni consola remota para diagnosticar | **Auditoría del grafo (sept 2026):** `WebGLRenderer({antialias:true, alpha:true, powerPreference:'high-performance'})` sin escalera de fallback; DPR fijo `min(dpr,2)`; sombras 2048 + MSAA (presupuesto GPU que iOS Safari/WKWebView aplica con más dureza); **sin manejadores `webglcontextlost/restored`** (iOS revoca contextos por memoria/al pasar a segundo plano → canvas negro permanente); **fuga de contextos**: `webgl2Disponible()` creaba un contexto nuevo en cada apertura sin perderlo (iOS limita los contextos vivos); cualquier fallo capturado dejaba el velo limpio y canvas en blanco en silencio; sin timeout del velo ni degradación | **Hardening aplicado (sept 2026):** (1) `badge-coin-3d.js` — sonda cacheada con `WEBGL_lose_context` en el canvas de prueba (sin fugas), nunca lanza (reporta por `opts.onFail`/`this.diag`); (2) `coin-engine.js` — escalera de creación en 3 tiers (`AA+high-performance` → `sin AA` → `sin AA/sin alpha`) con reintento desde tier 0, DPR adaptativo (iOS o memoria ≤ 4 GB → 1.5; tier 2 → 1.25), sombras 1024 en iOS móvil / desactivadas en tier 2 (`createCoinStudio` acepta `shadowMapSize`/`shadowsOn`), manejadores `webglcontextlost` (stop + evento UI) y `webglcontextrestored` (rebuild del entorno PMREM + reinicio del bucle), `destroy()` libera el backing store (8×8) sin perder el contexto (para reutilizar el canvas); (3) `app.js` — arranque `arrancarMoneda3D` con telemetría por etapas (`badge3d_open/module_ok/caps/engine/boot_ok/fail/context_lost_ui/timeout`), timeout del velo de 10 s con chip «Reintentar», chips de estado en el modal, **degradación 2D elegante** cuando WebGL2 no está disponible (iOS < 15 o contexto denegado: sello 2D grande en el escenario + nota), reconstrucción automática al volver a la app si el contexto se perdió (visibilitychange, una vez cada 6 s), **overlay de diagnóstico `?debug3d=1`** (eventos + caps + tier + UA, con botón Copiar); (4) `app.css` — bloque B-35 (chip, fallback 2D, panel debug) | ✅ Verificado en preview: apertura 3D real (tier 0, velo limpio, ready), eventos de etapa en el log, cierre limpio, fallback 2D forzado (canvas oculto + sello), reapertura 3D tras restaurar la sonda, ciclo simulado de pérdida/restauración de contexto (chip on → off, engine reanudado), consola limpia. Gates: 30/30 tests, sintaxis, check-imports OK. **Pendiente:** validación en iPhone real con `?debug3d=1` (volcado → afinar causa exacta); después cerrar |
| B-36 | ✅ | Navegación del visor desde el perfil por TODA la vitrina — **RESUELTO (refinamiento de B-34)** | Reportado en la app real (sept 2026): al abrir una insignia desde el apartado Insignias del perfil no aparecían los botones anterior/siguiente, a diferencia de colección | **Causa raíz:** B-34 filtraba la navegación del perfil por TEMA (solo insignias visibles de la misma sección); con una vitrina que muestra una insignia de cada sección (sello, día, racha…), cada lista quedaba en 1 → sin botones | **Resuelto (sept 2026):** en modo showcase (perfil propio o ajeno) la función construirNavegacionVisorInsignia ahora devuelve **toda la vitrina visible de ese perfil** (desbloqueadas y mostradas, en el orden de la vitrina, sin importar el tema); colección NO se toca (serie completa del tema con las cerradas) | ✅ Verificado en vivo con botones reales: vitrina propia multi-tema [sello-1, dia-1, racha-1] → abrir sello-1 «1 / 3» con siguiente ✓, siguiente → dia-1 «2 / 3» (ambos botones) ✓, siguiente → racha-1 «3 / 3» ✓, anterior → dia-1 ✓; perfil ajeno multi-tema idéntico; colección: sello-1 → «1 / 6» sin cambios ✓. Gates: 30/30 tests, sintaxis OK, check-imports OK. En disco — irá a producción en el próximo deploy |
| B-38 | ✅ | Toque sin interacción en insignias (iOS) — **RESUELTO (causa raíz encontrada con los informes del dispositivo)** | Reportado (sept 2026): el iPhone de una usuaria no reacciona AL PULSAR una insignia en Insignias ni en Colección — ni modal, ni spinner, ni chip | **Causa raíz (informes badge3d_js_error de su iPhone):** `Can't find variable: Notification` — ReferenceError sin capturar (unhandledrejection) en `solicitarPermisoNotificacionesWeb` (app.js:3955) llamado por `solicitarPermisoNotificaciones` (app.js:3947) durante el arranque. **iOS Safari no tiene la API `Notification`** (solo Android/PC), y el código evaluaba el identificador `Notification` sin guarda → excepción durante la inicialización que interrumpía el cableado de la interactividad en su iPhone; por eso el toque no hacía nada y además no llegaba ningún informe (el fallo ocurría antes del visor) | **Resuelto (sept 2026):** (1) guarda `typeof Notification !== 'undefined'` en `notificacionesWebDisponibles()` — todas las referencias (`solicitarPermisoNotificacionesWeb`, `inicializarPermisoNotificacionesWeb`, `lanzarNotificacionWeb`, `new Notification`) quedan detrás de esa guarda o de try/catch; (2) verificado que no hay otras APIs iOS-ausentes en el arranque (`navigator.vibrate` ya estaba guardado; sin showOpenFilePicker/MediaSession/wakeLock); (3) B-37 ya reporta cualquier rechazo JS del arranque | ✅ Gates: 30/30 tests, sintaxis OK, check-imports OK (12 módulos). **Desplegado:** producción sirve SW v163 con el fix (verificado en la URL pública) y cap:copy sincronizado (Android/iOS). Pendiente de validación en el iPhone de la usuaria (recarga → v163): el visor debe abrirse y los sensores confirman si queda algo |
| B-37 | ✅ | Diagnóstico automático del visor 3D desde el dispositivo (iOS sin consola) — **RESUELTO (instrumentación)** | Reportado (sept 2026): «a mi novia sigue sin funcionarle» el visor 3D en iPhone y no hay forma de ver el error desde Windows (sin consola remota) | **Resuelto (sept 2026):** nuevo `enviarDiagnostico3D()` en app.js — inserta un informe TÉCNICO sin PII en la tabla `badge3d_diags` de Supabase SIEMPRE que el visor falla (cualquier dispositivo) y en cada arranque con `?debug3d=1` (traza completa): UA, DPR, pantalla, webgl disponible, hardwareConcurrency, deviceMemory, modo, último error, rastro de los últimos 10 eventos badge3d_, caps de la sonda (renderer/version) y diag del engine (tier, pixelRatio, shadowMapSize); tope de 8 por sesión y nunca rompe el flujo. SQL: `supabase/sql/06-badge3d-diag.sql` (tabla + RLS insert-only para anon) | ✅ Verificado en preview (`?debug3d=1`): 3 informes enviados, llegada confirmada a Supabase (respuesta del servidor: «Could not find the table» → pendiente de ejecutar el SQL en el dashboard para que los insert tengan éxito). Gates: 30/30 tests, sintaxis OK, check-imports OK |
| B-28 | ✅ | Insignias de DÍA entregadas con 24 h de retraso — **RESUELTO** | Criterio de producto (sept 2026): una insignia de la sección Día no debe desbloquearse el mismo día del sellado — el día debe haber terminado por completo (24 h transcurridas); la notificación del banner se muestra al abrir la app / iniciar sesión | **Resuelto (sept 2026):** en `metricasInsignias()` el día de HOY ya no cuenta para `diasSellados` (`if (i > 0)` — solo días pasados terminados; la racha se mantiene igual); la sincronización de insignias se ejecuta al abrir la app (onload), al iniciar sesión (SIGNED_IN), al volver a la app (visibilitychange) y al recibir foco (focus) — `catalogoInsignias` solo encola el banner para desbloqueos **nuevos** (no se repite) | ✅ Verificado en preview: con hoy sellado `dias=0` (antes 1); al sellar un día completo de ayer `dias=1`; al abrir la app con un día pasado completado, `dia-1` se desbloquea y sale el banner «Nueva insignia de Día · Primer día» (evento `badge_unlocked` tras `app_open`). Gates: sintaxis OK, 30/30 tests. En disco — irá a producción en el próximo deploy |
| B-27 | 🟡 | Ficha del hábito sin botón × en el header | Auditoría de navegación (sept 2026): la ficha solo cierra con el `ios-back-btn` flotante, el botón físico Android, el gesto de borde iOS o ESC — en web de escritorio no hay cierre convencional visible en el header | Documentado (sept 2026): funcional en nativo; un × en el header sería más convencional — pendiente de decisión/implementación junto con el pulido visual | En disco — sin implementar (riesgo bajo) |
| B-09 | ✅ | ARIA y etiquetas — **RESUELTO** | Botones solo-icono sin `aria-label` (hamburguesa, cierre de modal) | Resuelto (Paso 6): `aria-label` en `btn-mensajes`, `btn-hamburger` y `close-modal`; toast ya tenía `role=status` ✓; visor (prev/next + canvas) ya etiquetado ✓ | ✅ Verificado: screen reader navega sin elementos sin nombre (pestañas con `role=tab`/`aria-selected` verificadas) |
| B-10 | 🟡 | CSS duplicado | 5.992 líneas con estilos copiados; bug del modal de tema derivó de ahí | Design system de primitivas (2.5) | Componentes nuevos con primitivas; ~30 % menos reglas |
| B-11 | 🟡 | Notificaciones con contenido fijado | Las locales fijan contenido al programarse; el resumen dominical puede llevar datos del último arranque | Documentar + decidir push (5.7) en F3; verificación en vivo cubre app abierta | Decisión 5.7 tomada; riesgo documentado |
| B-12 | 🟡 | Assets en 3 copias | `www/`, `ios/App/App/public`, `android/.../public` | `cap:copy` los sincroniza; add diff en F1 y en la beta F4 | `diff` limpio tras cada `cap:copy` |
| B-13 | 🟢 | Rueda de color recalculada | El modal de tema re-renderiza píxel a píxel al abrir | Pre-renderizar canvas offscreen (4.3) | Abrir modal sin recálculo (< 5 ms) |
| B-14 | 🟢 | Estado global implícito | `window.*` + variables de módulo; bugs de combinación de fases | Store mínimo (4.2) — se extrae en S1 del Paso 5 | 3 dominios leen/escriben por el store |
| B-15 | 🟢 | Dominios Vercel mezclados | `app-habitos.vercel.app` sirve una app ajena ("UKIFAZER"); aliases con SSO | Unificar/cancelar proyectos heredados; decidir alias/dominio público antes de F5 | Solo quedan los proyectos/dominios del plan |
| B-16 | 🟢 | i18n ES hardcodeado | 100 % de strings en español en el monolito | Extraer a `i18n.js` (5.6) junto con el refactor (S7) | Etiquetas clave en `i18n.js` (ES activo) |
| B-17 | 🟢 | Peso del shell 3D precacheado | ~487 KB de Three.js en el precache | Intencional (visor offline); evaluar compactación/LOD o cargar solo al abrir | Sin regresión en el visor offline tras optimizar |
| B-18 | ✅ | `awake-structured/` duplicado — **RESUELTO (promoción)** | Segunda copia del código (2,1 MB) con su propio `server.js`, tests y `vercel.json` | **Resuelto (sept 2026):** el responsable de producto eligió `awake-structured/` como fuente de verdad; se portaron los fixes del Paso 6 (B-04/B-05/accesibilidad), se validó 30/30 y se **promovió a `www/`** (layout por dominios). El árbol plano quedó archivado en `legacy/www/` (con README de archivo) | ✅ Verificado: `npm test` 30/30 sobre `www/`, `check-imports` OK, SW `awake-shell-v150`, pipeline raíz apuntando a `www/` |
| B-19 | ✅ | `www/.env.local` con `VERCEL_OIDC_TOKEN` — **RESUELTO** | No se servía (excluido por `www/.vercelignore` con `.env*`; 404 verificado); token distinto al raíz | Verificado (Paso 6): **sin referencias en el repo** (sin `.github`, sin CI, sin uso por el CLI local que lee la raíz) → **archivo eliminado de `www/`**. El `.env.local` raíz queda intacto | ✅ Cumplida: `www/.env.local` no existe; deploy v151 sin él |
| B-20 | ⛔ | `coin-studio.js` sin referencias — **REFUTADO** | El claim «sin referencias» era incorrecto: `coin-scene.js` importa `makeStudioEnv` de `../coin-studio.js` y `sw.js` lo precachea | Verificación del Paso 6: es dependencia viva del visor 3D (cadena `badge-coin-3d → coin-engine → coin-scene → coin-studio`) — **no eliminar** | Retirado del backlog |
| B-21 | 🟢 | notify-message: tokens FCM sin purga | Los tokens de `device_tokens` obsoletos se reintentan y fallan sin limpieza; sin retry/backoff | Cuando F3 decida push (5.7): purgar tokens inválidos (error `UNREGISTERED`) y reintento limitado | Errores de FCM manejados; `device_tokens` sin tokens muertos |

## 2. Orden de ataque sugerido

1. **Datos y producción primero**: B-01 → B-02 → B-03 (sin esto, la capa social y la confianza están en riesgo).
2. **Red de tests**: B-04 (antes de tocar más código de avisos).
3. **UX/accesibilidad**: B-07 → B-08 → B-09 → B-05.
4. **En paralelo con F1/F2 del roadmap**: B-06, B-11, B-12 como tareas de consolidación; B-13 a B-17 dentro del refactor (Paso 5) salvo decisión contraria.

## 3. Definición de "Paso 6 completado"

- B-01 a B-04 **resueltos y verificados**; B-05 a B-09 con acción acordada y ejecutada o fijada en la fase correspondiente.
- `npm test` ≥ 26 tests en verde; Lighthouse a11y ≥ 90; consola limpia (solo el 401 guest documentado).
- Coherente con la puerta **G2** del `docs/02-roadmap.md`.

*Regla del método: un fix por turno salvo que el bloque sea atómico (p. ej. aplicar `chat_reads.sql`), y siempre verificando tras cada cambio.*