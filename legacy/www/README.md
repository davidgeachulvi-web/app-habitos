# Árbol archivado (`legacy/www/`)

Versión anterior de la app (layout plano, monolito) — **archivada el 1 de septiembre de 2026** cuando la reorganización estructurada (antes `awake-structured/`) fue promovida como fuente de verdad a `www/` en la raíz, validada por el responsable de producto.

- Contiene el código desplegado hasta SW v149 y los cambios del Paso 6 anteriores a la promoción.
- **No se debe editar ni desplegar.** El árbol activo es `www/` en la raíz.
- Conservada por si se necesita consultar algo; se puede eliminar cuando se confirme que no hace falta (no hay git en este workspace, así que la copia es la única referencia histórica).
